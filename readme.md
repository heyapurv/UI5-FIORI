1. install ui5 cli 
npm init 
npm install --global @ui5/cli

create webapp -- root folder 
inside root folder (webapp) create index.html and manifest.json file
after that create ui5 yaml file -- ui5 init